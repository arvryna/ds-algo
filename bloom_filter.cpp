#include "bloom_filter.h"

#include <iostream>

/*
SPEC
****

use uniformly distributed hash function for BloomFilter

*/

template <typename Key, typename Value>
BloomFilter<Key, Value>::BloomFilter(const size_t n) {
}

int main() {
    // BloomFilter b_filter(10);
    return 0;
}